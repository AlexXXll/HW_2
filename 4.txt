#include <stdio.h>

int main()
{
    int k, m, h;
    
    printf("Введите секунду: ");scanf("%i", &k);
    
    k = k%86400; //В сутках всего 86400 секунд ставлю фильтр на 1-ю секунду новых суток
    h = k / 3600;
    m = (k%3600) / 60;
    printf("%d:%d\n", h, m);
    return 0;
}